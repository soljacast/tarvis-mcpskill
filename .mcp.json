{
  "mcpServers": {
    "tarvis": {
      "type": "http",
      "url": "${user_config.device_url}/api/agent/v1/mcp",
      "headers": {
        "Authorization": "Bearer ${user_config.device_token}"
      }
    }
  }
}
